[package]
name = "nexiscore_gui"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
eframe = "0.22"
egui = "0.22"
winapi = { version = "0.3", features = ["windef", "winuser"] }

[target.'cfg(windows)'.build-dependencies]
image = "0.24"
winres = "0.1"
